import styled from "styled-components";

export const ListWrapper = styled.div`
    padding: 20px 0;
    .songs-content {
        display: flex;
        flex-wrap: wrap;
        margin: 0 -21px;
    }
`